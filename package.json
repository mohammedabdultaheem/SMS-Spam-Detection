{
  "name": "sms-spam-detection",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev:frontend",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && .\\.venv\\Scripts\\activate && python main.py",
    "build:frontend": "cd frontend && npm install && npm run build",
    "install:all": "npm install --prefix frontend && cd backend && python -m venv .venv && .\\.venv\\Scripts\\activate && pip install -r requirements.txt"
  }
}
